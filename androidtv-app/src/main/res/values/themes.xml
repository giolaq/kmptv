<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- TV Theme — pure platform theme, no support library required.
         Compose applies its own Material3 theme at runtime; these values only
         cover the brief pre-Compose window shown during Activity startup. -->
    <style name="Theme.KMPTV.TV" parent="@android:style/Theme.Material.NoActionBar.Fullscreen">
        <item name="android:colorBackground">#FF121212</item>
        <item name="android:windowBackground">#FF121212</item>
        <item name="android:textColorPrimary">#FFFFFFFF</item>
    </style>
</resources>
